apiVersion: v1
kind: Service
metadata:
  name: cassandra-headless
spec:
  clusterIP: None
  ports:
  - port: 9042
  selector:
    app: cassandra
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cassandra
spec:
  serviceName: "cassandra-headless"
  replicas: 1
  selector:
    matchLabels:
      app: cassandra
  template:
    metadata:
      labels:
        app: cassandra
    spec:
      containers:
      - name: cassandra
        image: cassandra:4.1
        env:
        - name: MAX_HEAP_SIZE
          value: "512M" # kind / Docker Desktop 本地開發保守設定，先求穩定
        - name: HEAP_NEWSIZE
          value: "100M"
        ports:
        - containerPort: 9042
